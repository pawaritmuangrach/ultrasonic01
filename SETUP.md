# SETUP — ติดตั้งเครื่องมือ (ทำได้เลย ไม่ต้องมีอุปกรณ์)

ทุกอย่างในไฟล์นี้ทำได้โดยไม่ต้องมีฮาร์ดแวร์เลย ยกเว้นข้อ 5 ที่ต้องมี ESP32 + สายจัมป์ 1 เส้น

---

## 1. Arduino IDE + ESP32 core **3.x**

1. ติดตั้ง [Arduino IDE 2.x](https://www.arduino.cc/en/software)
2. `File → Preferences → Additional boards manager URLs` ใส่:
   ```
   https://espressif.github.io/arduino-esp32/package_esp32_index.json
   ```
3. `Tools → Board → Boards Manager` ค้น `esp32` → ติดตั้ง **esp32 by Espressif Systems เวอร์ชัน 3.x**

> ✅ **ขั้นตอนที่ 3 ทำให้แล้ว** — ติดตั้ง `esp32:esp32@3.3.11` ผ่าน arduino-cli ไว้ที่
> `C:\Users\User\AppData\Local\Arduino15` ซึ่งเป็นโฟลเดอร์เดียวกับที่ Arduino IDE ใช้
> **เปิด IDE ขึ้นมาจะเห็นบอร์ด ESP32 อยู่แล้ว ไม่ต้องโหลดซ้ำ**
>
> คอมไพล์จากบรรทัดคำสั่งได้ด้วย:
> ```
> arduino-cli compile --fqbn esp32:esp32:esp32 esp32_array/esp32_selftest
> arduino-cli upload  --fqbn esp32:esp32:esp32 -p COM3 esp32_array/esp32_selftest
> ```

> ⚠️ **ต้องเป็น 3.x เท่านั้น** — core 2.x สร้างบน ESP-IDF 4.x ซึ่งไม่มี API
> `esp_adc/adc_continuous.h` ที่โปรเจกต์นี้ใช้ทั้งหมด ถ้าเผลอติดตั้ง 2.x โค้ดจะคอมไพล์ไม่ผ่าน
> ด้วย error ประมาณ `esp_adc/adc_continuous.h: No such file or directory`

**ตั้งค่าบอร์ด** (`Tools`):

| ช่อง | ค่า |
|---|---|
| Board | ESP32 Dev Module |
| Upload Speed | 921600 |
| CPU Frequency | 240 MHz |
| Flash Frequency | 80 MHz |
| Partition Scheme | Default 4MB with spiffs |

---

## 2. ไดรเวอร์ USB-Serial (Windows)

ESP32 DevKit ใช้ชิปแปลง USB 2 แบบ **ดูที่ชิปเล็กๆ ใกล้พอร์ต USB บนบอร์ด**:

| ชิปบนบอร์ด | ไดรเวอร์ |
|---|---|
| **CH340 / CH9102** | [WCH CH341SER](https://www.wch-ic.com/downloads/CH341SER_EXE.html) |
| **CP2102 / CP2104** | [Silicon Labs CP210x VCP](https://www.silabs.com/developers/usb-to-uart-bridge-vcp-drivers) |

**วิธีเช็คว่าสำเร็จ**: เสียบ ESP32 → เปิด Device Manager → ต้องเห็น `USB-SERIAL CH340 (COM3)`
หรือ `Silicon Labs CP210x (COM3)` ใต้ `Ports (COM & LPT)`

**ถ้าไม่เห็นอะไรเลย** ให้สงสัยสาย USB ก่อนเสมอ — สาย micro-USB จำนวนมาก
**ต่อแค่ไฟ ไม่ต่อเส้นข้อมูล** เอาไว้ชาร์จอย่างเดียว นี่คือปัญหาอันดับหนึ่งของมือใหม่
ลองเปลี่ยนสายก่อนจะสงสัยว่าบอร์ดพัง

---

## 3. Python + ไลบรารี

ติดตั้ง [Python 3.10+](https://www.python.org/downloads/) — ตอนติดตั้ง **ติ๊ก "Add python.exe to PATH"**

```bash
pip install -r tools/requirements.txt
```

---

## 4. รัน scope ในโหมดจำลอง — ทำได้เดี๋ยวนี้เลย

```bash
python tools/scope_view.py --simulate
```

จะได้กราฟ 3 แผงที่แสดง **สิ่งที่วงจรที่ทำงานถูกต้องควรให้** ดูให้คุ้นก่อนที่จะได้เห็นของจริง
เพราะเวลาต่อวงจรจริงแล้วผิด คุณจะรู้ทันทีว่ามันผิดตรงไหนเมื่อเทียบกับภาพนี้

**สิ่งที่ต้องสังเกตให้เป็นในภาพจำลอง**

| แผง | สิ่งที่ต้องเห็น | ความหมาย |
|---|---|---|
| บน (raw) | ก้อนใหญ่ตอน t=0 แล้วค่อยๆ ซาลง | **ringing ของตัวส่ง** ไม่ใช่ echo — ตัวส่ง Q สูงจะสั่นต่ออีก ~0.4 ms หลังหยุดขับ ช่วงนี้มองอะไรไม่เห็น = **โซนบอด ~7 cm แรก** |
| กลาง (envelope) | ก้อนเล็กๆ ก้อนที่สอง มีลูกศร ▼ ชี้ | **นี่คือ echo จริง** ตำแหน่งตรงกับเส้นประแดง |
| ล่าง (FFT) | ยอดแหลมยอดเดียวที่ 40 kHz | ถ้าเห็นยอดที่ 50 Hz หรือ 100 Hz ด้วย = สายอินพุตยาวเกินไป รับไฟบ้าน |
| บน (แถบส้ม) | ช่วงที่เอาไปทำ FFT | ต้องคร่อม echo ไม่ใช่คร่อม ringing |

ลองเปลี่ยนระยะดูว่า echo ขยับตามสูตร `t = 2d/c` จริงไหม:

```bash
python tools/scope_view.py --simulate --distance-cm 50
```

**ผลที่ทดสอบแล้วว่าได้จริง:**

| ตั้งไว้ | echo ที่ควรอยู่ | ที่โปรแกรมวัดได้ | SNR |
|---|---|---|---|
| 20 cm | 1.166 ms | 23.5 cm | 29 dB |
| 30 cm | 1.749 ms | 33.2 cm | 30 dB |
| 50 cm | 2.915 ms | 53.2 cm | 30 dB |
| 100 cm | 5.831 ms | 102.8 cm | 28 dB |
| 300 cm | 17.493 ms | 303.2 cm | 11 dB |

> **สังเกตว่ามันเกินไป +3.2 cm ทุกระยะเท่ากันเป๊ะ — นี่ไม่ใช่บั๊ก**
> envelope ขึ้นยอดตอน**จบ**ชุดคลื่น 8 ลูก ไม่ใช่ตอนเริ่ม
> `8 รอบ / 40 kHz = 200 µs → 200µs × 343/2 = 3.43 cm` ตรงกับที่วัดได้
> **ค่าคงที่แบบนี้คือสิ่งที่การคาลิเบรตหักออกได้หมด** และ Stage 2 ที่ใช้
> cross-correlation กับ template จะไม่มีอคตินี้ตั้งแต่แรก
> — เจอ error คงที่ให้ดีใจ เจอ error ที่กระโดดไปมาถึงค่อยกังวล

**เกร็ดที่ฝังอยู่ในโค้ด**: ตัวหา echo ใช้ **CFAR** (เทียบแต่ละจุดกับพื้นหลังรอบข้าง)
ไม่ใช่ "หายอดที่สูงที่สุด" เพราะหางเสียงของตัวส่งจะสูงกว่า echo ไกลๆ เสมอ
สิ่งที่แยกสองอย่างนี้ออกจากกันคือ**รูปร่าง** ไม่ใช่ขนาด — หางเสียงลดลงเรื่อยๆ
ส่วน echo เป็นก้อนนูนที่โผล่ขึ้นมาเหนือเพื่อนบ้านของมันเอง

---

## 5. Self-test: พิสูจน์ว่า ESP32 สตรีม ADC ได้เร็วจริง

**ต้องมี**: ESP32 1 ตัว + สายจัมป์ตัวผู้-ตัวผู้ **1 เส้น** เท่านั้น

```
GPIO25  ────────►  GPIO34
```

GPIO34 เป็นขา input-only ต่อกลับด้านก็ไม่พัง

1. เปิด `esp32_array/esp32_selftest/esp32_selftest.ino` ใน Arduino IDE → อัปโหลด
2. เปิด Serial Monitor ที่ **921600 baud** → ควรเห็นข้อมูลชิปและ `# ready`
3. ปิด Serial Monitor (ไม่งั้นพอร์ตจะถูกจอง) แล้วรัน:

```bash
python tools/scope_view.py --port COM3
```

### เกณฑ์ผ่าน

| ข้อ | ต้องได้ |
|---|---|
| 1 | บรรทัด `# requested 1000.0 kS/s, achieved ___ kS/s` แสดง **≥ 95%** |
| 2 | FFT มียอดชัดที่ **~40 kHz** |
| 3 | ไม่มีข้อความ `!! channel mask mismatch` |
| 4 | สั่ง `f 2000000` ใน Serial Monitor แล้ววัดใหม่ → ดูว่า 2 MS/s ยังผ่านไหม |

### ✅ ผลจริงที่วัดได้แล้วบนบอร์ดของคุณ (ESP32-D0WD-V3 rev 301, COM5)

| ขอ | ได้จริง | % | bin ของ FFT | ความถี่ที่วัดได้ |
|---|---|---|---|---|
| 1,000 kS/s | **976.4** | 97.6% | 953 Hz | 39.02 kHz |
| 1,500 kS/s | **1,464.2** | 97.6% | 1,430 Hz | 39.04 kHz |
| **2,000 kS/s** | **1,951.9** | **97.6%** | 1,906 Hz | 39.03 kHz |

**ผ่านทุกข้อ** — และ 97.6% เท่ากันเป๊ะทั้งสามอัตรา แปลว่าส่วนที่หายไปคือ
**ค่าโสหุ้ยคงที่** (ก้อนแรกที่ทิ้งตอน DMA เริ่มทำงาน) ไม่ใช่เพดานของฮาร์ดแวร์
ถ้าเป็นเพดานจริง เปอร์เซ็นต์จะตกลงเมื่อขออัตราสูงขึ้น

**แผน Stage 3 เดินตามเดิมได้**: 1,951.9 ÷ 6 ช่อง = **325 kS/s ต่อช่อง = 8.1 จุดต่อคาบ 40 kHz**

> ความถี่ที่วัดได้ 39.0 kHz ไม่ใช่ 40.0 เพราะ**ตัวสร้างคลื่นของ DAC ปัดความถี่เป็นขั้น**
> ไม่ใช่ความผิดพลาดของการวัด — สังเกตว่าทั้งสามอัตราให้ตัวเลขตรงกันภายใน **±10 Hz**
> ทั้งที่ bin กว้างถึง 1,906 Hz ซึ่งเป็นผลของ **parabolic interpolation** ที่หาค่ายอดได้
> ละเอียดกว่า bin ประมาณ 100 เท่า — **เทคนิคเดียวกับที่ Stage 2 จะใช้หา Δt ระดับ sub-sample**

> **ตรวจจากไฟล์ของชิปแล้ว** (`soc_caps.h` ของ ESP32 ใน core 3.3.11):
> ```
> SOC_ADC_SAMPLE_FREQ_THRES_HIGH  = 2,000,000   ← 2 MS/s คือ "เพดานสูงสุด" ไม่ใช่ค่าที่สบาย
> SOC_ADC_SAMPLE_FREQ_THRES_LOW   =    20,000
> ```
> แปลว่าแผนที่วางไว้ (6 ช่อง × 333 kS/s) วิ่งอยู่ที่**ขีดสุดของฮาร์ดแวร์พอดี ไม่มีระยะเผื่อเลย**
> ข้อ 4 จึงสำคัญมาก — ถ้าได้จริงต่ำกว่า 2 MS/s ต้องปรับแผน Stage 3

**ข้อ 4 คือคำถามที่แพงที่สุดในโปรเจกต์นี้** — แผนทั้งหมดตั้งอยู่บนสมมติฐานว่า
ADC ทำ 2 MS/s ได้ ถ้าทำได้จริง → 6 ช่อง × 333 kS/s ตามแผน
ถ้าทำได้แค่ 1 MS/s → 6 ช่องเหลือ 167 kS/s/ช่อง = 4.2 sample ต่อคาบ 40 kHz
ซึ่งยังพอทำ cross-correlation ได้แต่ต้องพึ่ง interpolation หนักขึ้น
**รู้ตอนนี้ดีกว่ารู้ตอนบัดกรีเสร็จ 9 ช่องแล้ว**

### ถ้าไม่ผ่าน

| อาการ | สาเหตุที่น่าจะเป็น |
|---|---|
| `adc_continuous.h: No such file` | core เป็น 2.x → กลับไปข้อ 1 |
| `!! this is not an original ESP32` | ได้บอร์ด S3/C3 มา → ต้องเปลี่ยนบอร์ด |
| achieved ต่ำกว่า 90% | ลด `f` ลง แล้วหาเพดานจริงของบอร์ดคุณ |
| FFT ไม่มียอดที่ 40k แต่ raw เป็นเส้นตรง ~1.65 V | สายจัมป์ไม่แน่น หรือเสียบผิดขา |
| `!! bad magic` / `checksum` บ่อย | ลดบอดเป็น 460800 ทั้งใน .ino และ `--baud` |

---

## 6. PulseView สำหรับ Logic Analyzer

1. ติดตั้ง [PulseView (sigrok)](https://sigrok.org/wiki/Downloads)
2. เสียบ logic analyzer → **Windows จะยังไม่รู้จัก** ต้องใช้ [Zadig](https://zadig.akeo.ie/)
   เลือกอุปกรณ์ที่ขึ้นมา → เลือกไดรเวอร์ **WinUSB** → กด Replace Driver
3. เปิด PulseView → เลือก device `fx2lafw`

**ทดสอบว่าใช้ได้**: ต่อคีบ CH0 เข้า GPIO25 ของ ESP32 (ที่กำลังออกคลื่น 40 kHz จาก
self-test) และคีบ GND เข้า GND → กด Run ต้องเห็นคลื่นสี่เหลี่ยม 40 kHz

> ⚠️ **logic analyzer ตัวถูกทนแรงดันแค่ 5V และไม่มีการป้องกัน** ห้ามคีบเข้าไฟ 12V เด็ดขาด
> และ**ต้องต่อ GND ร่วมกันเสมอ**ก่อนคีบสัญญาณ

---

## 7. เช็ค breadboard ด้วยมัลติมิเตอร์ (ก่อนเสียบอะไรทั้งสิ้น)

ใช้โหมด continuity (ไอคอนคลื่นเสียง มีเสียงปี๊บ):

1. **รางไฟขาดกลางหรือเปล่า** — จิ้มหัวรางแดงกับท้ายรางแดง ถ้าไม่ปี๊บ = ขาด
   ต้องกระโดดสายเชื่อมเอง ทำทั้งรางแดงและรางน้ำเงิน **ทั้ง 3 บอร์ด**
2. **แถวไหนพังบ้าง** — สุ่มจิ้มรู a กับ e ของแถวเดียวกัน ต้องปี๊บ
3. **รางแดงกับรางน้ำเงินต้องไม่ปี๊บกัน** (ถ้าปี๊บ = บอร์ดลัดวงจร ทิ้งเลย)

ทำครั้งเดียว 5 นาที ประหยัดเวลาไล่บั๊กเป็นชั่วโมง

---

## ลำดับที่แนะนำ

```
วันนี้ (ไม่มีของ)   → ข้อ 1, 2, 3, 4, 6
ESP32 มาถึง        → ข้อ 5   ← สำคัญที่สุด
breadboard มาถึง    → ข้อ 7
ทรานสดิวเซอร์มาถึง  → เริ่ม Stage 1 จริง (hardware/rx_frontend.md)
```
