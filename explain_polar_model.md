# โมเดล PolarScan เทรนยังไง — อธิบายทีละขั้น

โมเดลนี้มาจาก `testmodel01/Ultrasonic_2D_Map-main` เทรนด้วยข้อมูล `walk_s1`–`walk_s5`
ของเราเอง ดูผลสดได้ที่ [`car/live_polar.py`](car/live_polar.py)

**สิ่งที่โมเดลทำ**: รับคลื่นเสียงดิบ 4 ช่อง → ตอบว่า *ในแต่ละทิศข้างหน้า พื้นผิวที่ใกล้ที่สุด
อยู่ไกลเท่าไหร่* แบ่งเป็น 13 ช่องมุม = แผนที่ย่อแบบเรดาร์

**สิ่งที่ต่างจาก `car/rules.py` ของเรา**: rules.py ตอบทิศของ *วัตถุเดียว* ด้วยสูตรเส้นตรง
2 ตัว ไม่มี ML เลย · PolarScan ตอบ *ทั้งแผนที่ 13 ช่อง* ด้วยนิวรัลเน็ต 594k พารามิเตอร์

---

## 1. ภาพรวมทั้งเส้นทาง

```
คลื่นดิบ 4x871         DSP (physics.py)        ฟีเจอร์             PolarNet          ผลลัพธ์
uint16 0-4095   ──►  กรองแบนด์ + เปลือกคลื่น ──► โปรไฟล์ 4x128 ──►  CNN + MLP  ──►  ระยะ 13 ช่อง
                     หายอดร่วม + TDOA          TDOA 6 · แรง 4       594k พารา.      + ธงว่ามีของไหม
                                                SNR 4 · ระยะ 1                      + วัตถุใกล้สุด

                                    กล้อง depth ──► เฉลย 13 ช่อง (ครู)
```

**กล้องเป็นครูตอนเทรนเท่านั้น** พอเทรนเสร็จโมเดลใช้เสียงอย่างเดียว

---

## 2. ขั้น DSP — แปลงคลื่นดิบเป็นฟีเจอร์ (`usmap/physics.py`)

ทำ **ก่อน** เข้านิวรัลเน็ต ไม่ได้เรียนรู้ เป็นฟิสิกส์ล้วน

### 2.1 กรองแบนด์ + หาเปลือกคลื่น

คลื่นดิบสั่นที่ 40 kHz แต่เราสนใจแค่ "ดังตอนไหน" ไม่สนลูกคลื่น

1. ลบค่ากลางออก แล้วกรองแบนด์รอบความถี่ที่ **aliased** ลงมา
   (สุ่มที่ 66 kHz ซึ่งต่ำกว่า Nyquist ของ 40 kHz — 40 kHz จึงพับลงมาที่ ~26 kHz)
2. **Hilbert transform** → เปลือกคลื่น (envelope) = เส้นความดังต่อเวลา

### 2.2 หายอดร่วม (common peak)

เป้าอยู่ที่ระยะเดียว ทุกช่องจึงควรดังพร้อมกัน — เลือก **ช่องที่เงียบที่สุด** เป็นตัวอ้างอิง
แล้วให้ทุกช่องอ่านที่หน้าต่างเดียวกัน

> ถ้าปล่อยให้แต่ละช่อง `argmax` เอง ช่องที่รบกวนสูงจะไปเกาะยอดปลอม
> (เราวัดไว้ในโปรเจกต์เดิม: แยกกันหา MAE 20.6 cm · ใช้ยอดร่วม 7.3 cm)

หน้าต่างค้นหาจำกัดที่ **40–200 cm** (`GATE_MIN_CM`/`GATE_MAX_CM`) และหักค่าหน่วงระบบ
**T0 = 1220 µs** ออกก่อนแปลงเป็นระยะ

### 2.3 TDOA — ผลต่างเวลาถึงระหว่างคู่ช่อง ⚠️ **จุดนี้เราแก้เอง**

ทิศทางซ่อนอยู่ใน *ผลต่างเวลา* ระหว่างหัวรับ คำนวณทั้ง 6 คู่ (จาก 4 ช่อง)

**ของเดิมใช้ไม่ได้** — วัดกับ walk_s1 แล้วพบว่า:

| อาการ | ตัวเลข |
|---|---|
| เฟรมที่ได้ 0 ทั้ง 6 คู่ | **44–60%** |
| ค่าที่ได้ตอนไม่เป็น 0 | ถึง **±782 µs** |
| เพดานทางฟิสิกส์ของอาเรย์ (กว้าง 12 cm) | **±350 µs** |

±782 µs = ทางเดินต่างกัน 27 cm บนอาเรย์ที่กว้างแค่ 12 cm → เป็นไปไม่ได้

**สาเหตุ 3 อย่าง และวิธีแก้:**

| ปัญหา | ทำไมพัง | แก้เป็น |
|---|---|---|
| ใช้ **GCC-PHAT** บนเปลือกคลื่น | PHAT หารด้วยขนาดสเปกตรัมเพื่อให้ยอดคม ได้ผลกับสัญญาณแบนด์กว้าง แต่เปลือกคลื่นเรียบมาก การหารแบบนั้นไปขยาย noise ความถี่สูงที่ไม่มีข้อมูลหน่วงเวลา | สหสัมพันธ์ไขว้แบบปกติ **ไม่ whiten** |
| ไม่จำกัดช่วง lag | zero-pad เป็น 128 จุดแล้ว argmax ทั้งเส้น → lag ออกได้ ±64 แซมเปิล ทั้งที่หน้าต่างซ้อนกันแค่ ±19 | ค้นเฉพาะ **±เพดานฟิสิกส์ของคู่นั้น** |
| ไม่มีย่อยแซมเปิล | 1 แซมเปิล = 15 µs หยาบมากเทียบกับช่วง ±73 µs ของคู่ที่ชิดกัน | **พาราโบลา**หาจุดยอด |

**ผลหลังแก้** (วัดกับ walk_s3 273 เฟรม):

| คู่ช่อง | corr กับมุมกล้อง เดิม | **แก้แล้ว** |
|---|---|---|
| 0-1 | −0.417 | **−0.674** |
| 0-2 | −0.283 | **−0.589** |
| 0-3 | −0.178 | **−0.281** |
| 1-2 | +0.051 | **+0.246** |
| 1-3 | +0.250 | **+0.607** |
| 2-3 | +0.138 | **+0.482** |

- เฟรมที่ได้ 0 ทั้ง 6 คู่: **44% → 0%**
- ใช้ TDOA ทั้ง 6 คู่ฟิตมุมเชิงเส้นล้วน ๆ ได้ **6.03°** (เดามั่วได้ 10.70°)
- **นอร์มัลไลซ์เป็น −1..+1** ก่อนป้อนโมเดล (หารด้วยเพดานของคู่ตัวเอง)
  ของเดิมป้อนไมโครวินาทีดิบถึง ±782 เข้าชั้น Linear ที่ฟีเจอร์อื่นอยู่แถว 0–25
  ตัวที่ใหญ่กว่าสิบเท่าจะครอบงำการเรียนรู้ช่วงต้น

> ต้นฉบับเก็บไว้ที่ `usmap/physics.py.orig` เทียบได้ตลอด

### 2.4 โปรไฟล์ระยะ (range profile) — ฟีเจอร์ที่ใหญ่ที่สุด

เปลือกคลื่นของแต่ละช่องถูก **รีแซมเปิลเป็น 128 ช่องระยะ** ครอบคลุม 40–200 cm
(ช่องละ ~1.25 cm) แล้วหารด้วยค่าสูงสุดของช่องตัวเอง

นี่คือจุดที่ต่างจาก `rules.py` ของเรามากที่สุด — **เน็ตเห็นรูปร่างเอคโค่ทั้งเส้น**
ไม่ใช่แค่ยอดค่าเดียว ความกว้างของเอคโค่และ multipath พาข้อมูลมาด้วย

### 2.5 สรุปฟีเจอร์ที่เข้าโมเดล

| ฟีเจอร์ | ขนาด | คือ |
|---|---|---|
| `prof` | **4 × 128** | โปรไฟล์ระยะรายช่อง |
| `tdoa` | 6 | ผลต่างเวลาแต่ละคู่ (−1..+1) |
| `amps` | 4 | ความแรงยอด หารด้วยตัวมากสุด |
| `snr` | 4 | ยอด ÷ พื้นเสียงรบกวน |
| `dist_cm` | 1 | ระยะจากฟิสิกส์ล้วน (÷200 ก่อนเข้า) |

---

## 3. เฉลย — กล้อง depth เป็นครู (`usmap/polar_gt.py`)

แปลงภาพ depth 320×240 เป็น **ระยะที่ใกล้ที่สุดใน 13 ช่องมุม**

1. ตัดเฉพาะ **แถบกลางแนวตั้ง 40%** (ตัดพื้น/เพดานที่เสียงไม่เคยได้ยิน)
2. **ย้อนภาพผ่านโมเดลรูเข็ม** — คำนวณมุมจริงของทุกพิกเซล
   `fx = w / (2·tan(FOV/2))` แล้ว `มุม = atan2(X, Z)`
   (นี่ดีกว่าการหั่นตามคอลัมน์พิกเซล เพราะมุมกับคอลัมน์ไม่ได้เป็นเส้นตรงต่อกัน)
3. เชื่อเฉพาะพิกเซลที่ระยะ **400–2500 mm**
4. แต่ละช่องมุมเอา **เปอร์เซ็นไทล์ที่ 5** ของระยะ (ไม่ใช่ min เพราะ min ไวต่อพิกเซลเสียตัวเดียว)
5. ช่องที่มีพิกเซลน้อยกว่า 10 → ตั้งธง `valid = False`

พร้อมกันนั้นคำนวณ **"วัตถุใกล้สุด"** = ระยะ+มุมของช่องที่ใกล้ที่สุด ไว้เป็นตัวชี้วัด

### ⚠️ เฉลยมุมนี้มีปัญหาแบบเดียวกับที่เราเคยเจอ

"มุมของช่องที่ใกล้ที่สุด" คือ **argmin ข้ามช่อง** ซึ่งเป็นบั๊กเดียวกับที่เราแก้ไปใน
`car/labels.py` — เมื่อคนกินภาพหลายช่องที่ระยะเท่ากัน argmin จะเลือกช่องแบบสุ่ม

วัดบนเฟรมเดียวกัน 423 เฟรมของ walk_s5:

| เฉลย | กระโดดต่อเฟรม (มัธยฐาน) | เฟรมที่กระโดด >5° |
|---|---|---|
| ของเขา (argmin 13 ช่อง) | **4.49°** | **38%** |
| ของเรา (`target_angle` จุดศูนย์กลางก้อนเป้า) | 0.26° | 0% |

สองเฉลย corr 0.864 = วัดสิ่งเดียวกัน แต่ของเขากระโดด 17 เท่า
**คนขยับ 4.5° ใน 1/15 วินาทีซ้ำ ๆ ไม่ได้** → นั่นคือ noise ในเฉลย
ซึ่ง **ตั้งเพดานให้ MAE ต่ำกว่านั้นไม่ได้ ไม่ว่าโมเดลจะดีแค่ไหน**

---

## 4. สถาปัตยกรรม PolarNet (`usmap/polar_model.py`)

**593,628 พารามิเตอร์** — เล็กมาก เทรนบน CPU ได้สบาย

```
prof (4,128) ──► Conv1d 4→32  k7 ──► ReLU
                 Conv1d 32→32 k5 ──► ReLU
                 Conv1d 32→64 k5 stride2 ──► ReLU   (128→64)
                 Conv1d 64→64 k3 stride2 ──► ReLU   (64→32)
                 Flatten → 2048
                                    │
     tdoa(6) + amps(4) + snr(4) + dist(1) ──┤ ต่อท้าย → 2063
                                    │
                            Linear 2063→256 ─ ReLU ─ Dropout 0.2
                            Linear 256→128  ─ ReLU
                                    │
        ┌───────────────┬───────────┼──────────────┬──────────────┐
   bin_dist(13)    bin_valid(13)              near_d(1)       near_a(1)
   ระยะแต่ละช่อง   ธงว่ามีของไหม              ระยะใกล้สุด      มุมใกล้สุด
```

| ส่วน | พารามิเตอร์ |
|---|---|
| CNN เข้ารหัสโปรไฟล์ | 28,736 |
| **หัว MLP** | **561,280** (95% อยู่ตรงนี้ — ชั้น 2063→256) |
| หัวออก 4 ตัว | 3,612 |

---

## 5. ฟังก์ชันสูญเสีย

```python
loss = masked_bin_loss + 0.5 · L1(near_d) + 2.0 · |near_a| / 30
```

โดย `masked_bin_loss = L1(ระยะเฉพาะช่องที่ valid) + 0.5 · BCE(ธง valid)`

จุดสำคัญ 3 อย่าง:

1. **มาสก์** — เฉพาะช่องที่กล้อง *เห็นจริง* เท่านั้นที่มีส่วนในการคิด loss ของระยะ
   ช่องที่กล้องไม่เห็น (66% ของช่องทั้งหมดในชุดนี้เห็น) จะไม่ถูกลงโทษ
   แต่ยังต้องทายธง `valid` ให้ถูก ซึ่งคิดด้วย BCE แยก
2. **L1 ไม่ใช่ L2** — ทนต่อค่าผิดปกติมากกว่า ซึ่งจำเป็นเพราะเฉลยมี noise
3. **น้ำหนักมุม ×2 และหารด้วย 30** — มุมมีหน่วยองศา (±30) ระยะมีหน่วย cm (40–200)
   ถ้าไม่ปรับสเกล ระยะจะครอบงำ loss ทั้งหมด

---

## 6. การแบ่งข้อมูลและลูปเทรน (`usmap/polar_train.py`)

| ชุด | ช่วง | เฟรม |
|---|---|---|
| เทรน | `walk_s1` + `walk_s2` + `walk_s3` | 2,617 |
| ตรวจ (val) | `walk_s4` | 857 |
| **ทดสอบ (test)** | `walk_s5` | 851 |

**แบ่งตามช่วงการเดิน ไม่ใช่สุ่มรายเฟรม** — เฟรมติดกันเหมือนกันแทบทุกอย่าง
ถ้าสุ่มแบ่ง คำตอบจะรั่วจากชุดเทรนเข้าชุดทดสอบ แล้วตัวเลขจะสวยเกินจริง

```
optimizer  AdamW  lr 1e-3  weight_decay 1e-4
scheduler  CosineAnnealingLR ตลอด 60 เอพอค
batch      32
เซฟ        เอพอคที่ near-distance MAE บน val ต่ำที่สุด
```

**เอพอคแรกช้า** (ประมาณ 1 นาที — 2,617 เฟรม × 25 ms) เพราะต้องคำนวณ DSP ทุกเฟรม เอพอคถัดไปเร็วมาก
เพราะ `PolarDataset` แคชผลไว้ในหน่วยความจำ · ทั้งหมด ~4 นาทีบน CPU

---

## 7. ผลจริงที่วัดได้ พร้อมเส้นฐานที่ควรเทียบ

ทดสอบกับ `walk_s5` ที่โมเดลไม่เคยเห็น:

| วิธี | ระยะ MAE | มุม MAE |
|---|---|---|
| **ทายค่าเฉลี่ยชุดเทรน (ไม่ดูข้อมูลเลย)** | **5.53 cm** | **15.50°** |
| ฟิสิกส์ล้วน (argmax + T0) | 12.07 cm | — |
| PolarNet (TDOA เดิมที่พัง) | 4.52 cm | 8.90° |
| **PolarNet (TDOA แก้แล้ว, 3 รัน)** | **4.12–4.47 cm** | 9.69–10.21° |

**อ่านตารางนี้ให้ถูก:**

- **ระยะ ชนะการเดามั่วแค่ ~1 cm** เพราะผู้เก็บยืนระยะเดียวเกือบตลอด (sd 5.8 cm)
  **ไม่มีอะไรให้ทำนาย** · ที่ README ต้นทางเทียบกับ "ฟิสิกส์ 12 cm" ดูดีมาก
  แต่ฟิสิกส์**แย่กว่าการเดาค่าเฉลี่ย** การเทียบแบบนั้นทำให้โมเดลดูเก่งเกินจริง
- **มุม ชนะจริง 34–37%** (15.5° → 9.7–10.2°) อันนี้ของจริง
- **แก้ TDOA แล้วมุมกลับแย่ลงเล็กน้อย** (8.90° → 9.86°) เทรน 3 รอบยืนยันว่าไม่ใช่ความบังเอิญ
  เดิมโมเดลเมินฟีเจอร์ที่เสียแล้วพึ่ง `prof`+`amps` ซึ่งนิ่งกว่า พอ TDOA ใช้ได้
  โมเดลหันไปพึ่งมัน แต่อาเรย์แค่ 12 cm ทำให้ TDOA ยังหยาบ
  (**ablation**: ตัด TDOA ทิ้ง มุมแย่ลง 2.42° — เดิมแย่ลงแค่ 0.33° = ไม่ได้ใช้เลย)

### แผนที่ทั้ง 13 ช่อง — ตัวชี้วัดที่ตรงเป้าหมายจริง

| วิธี | MAE ต่อช่อง |
|---|---|
| ทายเลขเดียวทุกช่อง | 24.33 cm |
| **ทายค่าเฉลี่ยของช่องนั้น ๆ (ไม่ฟังเสียง)** | **19.09 cm** |
| **PolarNet** | **15.43 cm** |

ดีกว่าการไม่ฟังเสียงเลย **19%** เท่านั้น · ดูรายช่องจะเห็นว่าทำไม:

| ช่วงช่อง | กล้องเห็น | ค่าจริงแกว่ง | PolarNet | เดาค่าเฉลี่ย |
|---|---|---|---|---|
| ซ้าย (−27°…−9°) | 100% | 20–44 cm | **11–39 cm** | 19–42 cm |
| กลาง-ขวา (0°…+27°) | 18–59% | 3–9 cm | 4–7 cm | 4–7 cm |

**ฝั่งขวาโมเดลไม่ได้เรียนอะไรเลย** เพราะค่าจริงแทบไม่แกว่ง

---

## 8. เพดานสองชั้นที่โมเดลข้ามไม่ได้

### ชั้นที่ 1 — ชุดข้อมูล (แก้ได้)

ชุด `walk` มี **คนเดียว ที่ระยะเดียว ฉากหลังไม่เปลี่ยน** จะสอนให้ "ทำแผนที่ห้อง" ไม่ได้
เพราะไม่มีห้องที่เปลี่ยนให้เรียน

**ต้องเก็บข้อมูลที่ฉากเปลี่ยนจริง** — ย้ายแผงไปรอบห้อง วางของหลายชิ้นคนละระยะ
มีผนังใกล้/ไกลสลับกัน

### ชั้นที่ 2 — ฟิสิกส์ (แก้ด้วยฮาร์ดแวร์เท่านั้น)

```
λ ที่ 40 kHz = 8.6 mm     ความละเอียดเชิงมุม ≈ λ / ความกว้างอาเรย์
```

| อาเรย์ | ความกว้าง | ความละเอียด |
|---|---|---|
| ที่ใช้อัด walk | 12 cm | **4.1°** |
| `plate_mini` ใหม่ | 11 cm | 4.5° |
| แผงเต็ม 9 RX | 24 cm | **2.0°** |

13 ช่องบน 58.4° = 4.5°/ช่อง — **ชนเพดานของอาเรย์ 12 cm พอดีแล้ว**
แบ่งช่องถี่กว่านี้ไม่ได้ข้อมูลเพิ่ม ต้องขยายความกว้างอาเรย์อย่างเดียว
แผงเต็ม 9 RX ให้ 2.0° = ดีขึ้น **2 เท่า** และแบ่งได้ ~29 ช่อง

---

## 9. รันเอง

```bash
cd testmodel01/Ultrasonic_2D_Map-main/Ultrasonic_2D_Map-main
python -m usmap.polar_train          # เทรนใหม่ ~4 นาที -> output/models/polarscan.pt
```

ดูผล:

```bash
python car/live_polar.py --replay walk_s5     # เรดาร์สด จากข้อมูลที่อัดไว้
python car/live_polar.py --port COM5          # ต่อฮาร์ดแวร์จริง
```

> `dataset/walk_s1..s5` เป็น junction ชี้ไปที่ `car/data/` แล้ว ไม่ต้องคัดลอกข้อมูล
